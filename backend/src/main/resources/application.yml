spring:
  application:
    name: lost-city-backend

  data:
    mongodb:
      uri: mongodb+srv://shashikumarezhil_db_user:CZHLkjeFvHz55vC5@lostcity.1g3rszx.mongodb.net/lostcity?retryWrites=true&w=majority&appName=LostCity
      database: lostcity

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-change-this-in-production-must-be-very-long}
  expiration: 86400000 # 24 hours in milliseconds

cors:
  allowed-origins: http://localhost:3000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
